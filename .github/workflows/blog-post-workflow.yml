name: Latest blog post workflow
# on:
#   workflow_dispatch:
#   schedule:
#     # Runs daily at 00:30 UTC (After main.yml and Waka-Readme)
#     - cron: '30 0 * * *'
permissions:
  contents: write
jobs:
  update-readme-with-blog:
    name: Update this repo's README with the latest blog posts
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is currently disabled pending a valid RSS feed."
#       - uses: actions/checkout@v4
#       - uses: gautamkrishnar/blog-post-workflow@v1
#         with:
#           max_post_count: "5"
#           retry_count: 3
#           retry_wait_time: 1
#           # TODO: USER - Replace with your own RSS feed URL (e.g., https://dev.to/feed/yourusername or https://medium.com/feed/@yourusername)
#           feed_list: "https://dev.to/feed/apoorvtyagi"
